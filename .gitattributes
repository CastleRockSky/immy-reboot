# Normalize line endings to LF in the repo and on every checkout. PowerShell
# reads both LF and CRLF fine; pinning LF avoids platform-dependent dist hashes
# in CI.
* text=auto eol=lf
*.ps1 text eol=lf
*.md  text eol=lf
*.yml text eol=lf
